/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com' , 'plus.unsplash.com' , 'imgur.com', 'i.imgur.com'], // Add the hostname here
    },
  };
  
  module.exports = nextConfig;